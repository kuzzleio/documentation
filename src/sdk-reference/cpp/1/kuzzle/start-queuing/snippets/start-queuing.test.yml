name: kuzzle#startQueuing
description: Starts the requests queuing
hooks:
  before:
  after:
template: without-connect
expected: Kuzzle will auto queue next request sent in 'disconnected' state
sdk: cpp
version: 1

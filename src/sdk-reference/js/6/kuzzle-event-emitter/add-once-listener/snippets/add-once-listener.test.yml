name: kuzzleEventEmitter#add-once-listener
description: Adds new once listener for the given event
hooks:
  before:
  after:
template: eventemitter
expected: Successfully added 2 new once listeners
sdk: js
version: 6

version: 0.2

phases:
  install:
    commands:
      - docker pull kuzzleio/documentation-v2:js
      - sysctl -w vm.max_map_count=262144
  build:
    commands:
      - docker run -a stdout -a stderr --rm --privileged --mount type=bind,source="$(pwd)",target=/app kuzzleio/documentation-v2:js
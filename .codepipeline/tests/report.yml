version: 0.2

phases:
  install:
    commands:
      - sleep 5
  build:
    commands:
      - echo $CODEBUILD_SOURCE_VERSION
      - echo $CODEBUILD_SOURCE_REPO_URL
      - echo $CODEBUILD_RESOLVED_SOURCE_VERSION
